# Production Environment Configuration Template
# Copy this file to .env for production deployment
# CRITICAL: Update all values before deploying to production
# DO NOT commit this file with real values to version control

# =============================================================================
# ENVIRONMENT CONFIGURATION
# =============================================================================
APP_ENVIRONMENT=production
APP_PRODUCTION=true
APP_DEBUG_ENABLED=false
APP_VERSION=1.0.0
APP_BUILD_TIMESTAMP=2024-01-01T00:00:00Z

# =============================================================================
# JWT CONFIGURATION (CRITICAL - MUST BE CHANGED)
# =============================================================================
# SECURITY REQUIREMENT: Must be at least 32 characters with high entropy
# Generate using: openssl rand -base64 48
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_VALUE_AT_LEAST_32_CHARACTERS_LONG_WITH_HIGH_ENTROPY
JWT_EXPIRATION=PT30M
JWT_REFRESH_EXPIRATION=P1D
JWT_ISSUER=rct-backend-production
JWT_AUDIENCE=rct-frontend-production

# =============================================================================
# DATABASE CONFIGURATION (PRODUCTION)
# =============================================================================
SPRING_DATASOURCE_URL=jdbc:postgresql://your-production-db-host:5432/rctdb_prod
SPRING_DATASOURCE_USERNAME=rct_prod_user
SPRING_DATASOURCE_PASSWORD=CHANGE_THIS_TO_A_SECURE_DATABASE_PASSWORD
SPRING_DATASOURCE_DRIVER=org.postgresql.Driver
SPRING_JPA_DIALECT=org.hibernate.dialect.PostgreSQLDialect

# Database Pool Configuration (Production Optimized)
DATABASE_USE_CUSTOM_POOL=true
DATABASE_CONNECTION_POOL_SIZE=25
DATABASE_CONNECTION_TIMEOUT=PT30S
DATABASE_IDLE_TIMEOUT=PT10M
DATABASE_MAX_LIFETIME=PT30M
DATABASE_SHOW_SQL=false

# =============================================================================
# CORS CONFIGURATION (PRODUCTION - SPECIFIC DOMAINS ONLY)
# =============================================================================
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS
CORS_ALLOWED_HEADERS=Authorization,Content-Type,X-Requested-With
CORS_ALLOW_CREDENTIALS=true
CORS_MAX_AGE=PT1H

# =============================================================================
# SECURITY CONFIGURATION (PRODUCTION HARDENED)
# =============================================================================
SECURITY_PASSWORD_MIN_LENGTH=12
SECURITY_PASSWORD_MAX_LENGTH=256
SECURITY_MAX_LOGIN_ATTEMPTS=3
SECURITY_LOCKOUT_DURATION=PT30M
SECURITY_REQUIRE_STRONG_PASSWORD=true

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
SERVER_PORT=8080

# =============================================================================
# LOGGING CONFIGURATION (PRODUCTION)
# =============================================================================
LOGGING_LEVEL_COM_RCT=WARN
LOGGING_LEVEL_ROOT=ERROR
LOGGING_LEVEL_ORG_SPRINGFRAMEWORK=ERROR
LOGGING_LEVEL_ORG_HIBERNATE=ERROR

# =============================================================================
# MONITORING AND OBSERVABILITY (Optional)
# =============================================================================
# Uncomment and configure if using external monitoring services
# MONITORING_SERVICE_URL=https://your-monitoring-service.com
# MONITORING_API_KEY=your-monitoring-api-key
# METRICS_EXPORT_ENABLED=true
# HEALTH_CHECK_ENABLED=true

# =============================================================================
# EXTERNAL SERVICES (Optional)
# =============================================================================
# Configure if your application integrates with external services
# EMAIL_SERVICE_API_KEY=your-email-service-api-key
# SMS_SERVICE_API_KEY=your-sms-service-api-key
# STORAGE_SERVICE_ACCESS_KEY=your-storage-access-key
# STORAGE_SERVICE_SECRET_KEY=your-storage-secret-key

# =============================================================================
# SECURITY HEADERS AND FEATURES (Recommended)
# =============================================================================
# These should be configured at the reverse proxy/load balancer level
# but can also be set here for application-level enforcement
# SECURITY_HEADERS_ENABLED=true
# HSTS_ENABLED=true
# CONTENT_SECURITY_POLICY_ENABLED=true
# X_FRAME_OPTIONS=DENY
# X_CONTENT_TYPE_OPTIONS=nosniff

# =============================================================================
# PRODUCTION DEPLOYMENT CHECKLIST
# =============================================================================
# Before deploying to production, ensure:
# 
# □ All CHANGE_THIS_* values have been replaced with secure values
# □ JWT_SECRET is at least 32 characters with high entropy
# □ Database credentials are unique and secure
# □ CORS origins are specific production domains (no wildcards)
# □ Debug mode is disabled (APP_DEBUG_ENABLED=false)
# □ Production flag is enabled (APP_PRODUCTION=true)
# □ All localhost references are replaced
# □ HTTPS is enforced at the infrastructure level
# □ Database connections use SSL/TLS
# □ Firewall rules are configured
# □ Monitoring and alerting are set up
# □ Backup procedures are in place
# □ Incident response procedures are documented
# □ Security scanning has been performed
# □ Load testing has been completed
# □ Disaster recovery procedures are tested
# 
# SECURITY REMINDER:
# - Never commit this file with real values to version control
# - Use a secure secret management system in production
# - Implement regular secret rotation
# - Monitor for security vulnerabilities
# - Keep dependencies up to date
# - Perform regular security audits