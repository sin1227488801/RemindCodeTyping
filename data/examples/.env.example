# Example environment configuration file
# Copy this file to .env and update with your actual values
# DO NOT commit .env files to version control

# Environment Configuration
APP_ENVIRONMENT=dev
APP_PRODUCTION=false
APP_DEBUG_ENABLED=true
APP_VERSION=1.0.0-SNAPSHOT
APP_BUILD_TIMESTAMP=2024-01-01T00:00:00Z

# JWT Configuration (CRITICAL: Change these for production!)
JWT_SECRET=your-super-secret-jwt-key-that-is-at-least-32-characters-long-and-secure
JWT_EXPIRATION=PT60M
JWT_REFRESH_EXPIRATION=P7D
JWT_ISSUER=rct-backend
JWT_AUDIENCE=rct-frontend

# Database Configuration (Production)
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/rctdb
SPRING_DATASOURCE_USERNAME=rct_user
SPRING_DATASOURCE_PASSWORD=your-secure-database-password
SPRING_DATASOURCE_DRIVER=org.postgresql.Driver
SPRING_JPA_DIALECT=org.hibernate.dialect.PostgreSQLDialect

# Database Pool Configuration
DATABASE_USE_CUSTOM_POOL=true
DATABASE_CONNECTION_POOL_SIZE=20
DATABASE_CONNECTION_TIMEOUT=PT30S
DATABASE_IDLE_TIMEOUT=PT10M
DATABASE_MAX_LIFETIME=PT30M
DATABASE_SHOW_SQL=false

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com
CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS
CORS_ALLOWED_HEADERS=*
CORS_ALLOW_CREDENTIALS=true
CORS_MAX_AGE=PT1H

# Security Configuration
SECURITY_PASSWORD_MIN_LENGTH=8
SECURITY_PASSWORD_MAX_LENGTH=128
SECURITY_MAX_LOGIN_ATTEMPTS=5
SECURITY_LOCKOUT_DURATION=PT15M
SECURITY_REQUIRE_STRONG_PASSWORD=true

# Server Configuration
SERVER_PORT=8080

# Logging Configuration (optional overrides)
LOGGING_LEVEL_COM_RCT=INFO
LOGGING_LEVEL_ROOT=WARN

# Additional Security Configuration (Production)
# Set these in production for enhanced security

# External Service API Keys (if needed)
# EXTERNAL_API_KEY=your-external-service-api-key
# MONITORING_SERVICE_KEY=your-monitoring-service-key
# EMAIL_SERVICE_API_KEY=your-email-service-api-key

# Production Security Notes:
# 1. JWT_SECRET must be at least 32 characters and have high entropy
# 2. Database passwords should be complex and unique
# 3. CORS origins should be specific domains, not wildcards
# 4. All secrets should be stored securely (e.g., Azure Key Vault, AWS Secrets Manager)
# 5. Enable HTTPS in production
# 6. Set APP_PRODUCTION=true and APP_DEBUG_ENABLED=false for production
# 7. Regularly rotate secrets and credentials
# 8. Use environment-specific configuration files
# 9. Never commit .env files to version control
# 10. Implement proper secret scanning in CI/CD pipelines

# Security Checklist for Production:
# □ JWT_SECRET is at least 32 characters with mixed case, numbers, and symbols
# □ Database credentials are unique and complex
# □ CORS origins are specific production domains only
# □ Debug mode is disabled (APP_DEBUG_ENABLED=false)
# □ Production flag is enabled (APP_PRODUCTION=true)
# □ All localhost references are replaced with production hostnames
# □ HTTPS is enforced for all endpoints
# □ Security headers are configured
# □ Rate limiting is enabled
# □ Monitoring and alerting are configured
# □ Backup and disaster recovery procedures are in place
# □ Secret rotation procedures are documented and implemented